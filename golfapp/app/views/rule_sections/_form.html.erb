<%= form_with(model: rule_section) do |form| %>
  <% if rule_section.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rule_section.errors.count, "error") %> prohibited this rule_section from being saved:</h2>

      <ul>
        <% rule_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :text_content, style: "display: block" %>
    <%= form.textarea :text_content %>
  </div>

  <div>
    <%= form.label :source_url, style: "display: block" %>
    <%= form.text_field :source_url %>
  </div>

  <div>
    <%= form.label :parent_id, style: "display: block" %>
    <%= form.text_field :parent_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
